version: '3.6'

services:

  mytheresa:
    container_name: mytheresa
    logging:
      options:
        max-size: 2048m
        max-file: "2"
    build:
      context: .
    ports:
      - $APP_PORT:8000
    volumes:
      - gopath:/go
    network_mode: bridge


volumes:
  gopath: